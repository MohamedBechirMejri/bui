import { motion } from "framer-motion";

export const ComingUpNext = () => {
  return (
    <motion.div
      style={{
        width: 500,
        border: "1px solid black",
      }}
    >
      tetst
    </motion.div>
  );
};
